name: Block Fuel

on:
  workflow_dispatch:
  schedule:
    - cron: "*/15 * * * *"

jobs:
  block-fuel:
    runs-on: ubuntu-latest

    steps:
      - name: Test automatic run
        run: |
          echo "🔥 BLOCK FUEL AUTOMATION"
          echo "Started: $(date)"
          echo "Generating a Minecraft Short..."
